import settings

def generate(bld_area):
    cnt_helipads = settings.settings["helipad-cnt"]

    #random corder of bld
    #save locations of all possible corners?
    #neighbors shoudl be lower... can we keep track?? return max corner height?
    #... or just put it on top within bounding rect (thus save height + bld_area

    #heli min size max size